$(document).ready(function(){
	// $('.commissions .has_many_add').one('click', function(){
	// 	var comm_length = $('.commissions fieldset').length
	// 	var checkCommLength = setInterval(function() {
	// 		if ( $('.commissions fieldset').length != comm_length ) {
	// 			comm_length = $('.commissions fieldset').length;		
	// 			$('.commissions .has_many_add').click();
	// 			if (comm_length === 4){
	// 				clearInterval(checkCommLength);
	// 			}
	// 		}
	// 	}, 100);
	// });	
})
